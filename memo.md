# 三合鏡(さんごうきょう)

**現行版: ver 1.1**

外三合を映す鏡 — 薪傳鄭子太極拳の練習動画を解析する個人用アプリ。

スマホで撮影した套路の動画にAIポーズ推定(MediaPipe)を重ね、鏡や内部感覚だけでは確認しづらい **外三合の一致** と **立身中正** を目で確かめるための道具。

- 本体: `index.html` 1ファイル(依存なし、ビルド不要)
- 動作: スマホ/PCのブラウザのみ。解析は端末内で完結し、動画はどこにも送信されない
- 費用: ゼロ(GitHub Pages + MediaPipe、いずれも無料)

## このアプリが見るもの・見ないもの

**見るもの(外三合と功架):**

| ペア | 結ぶ点 | 判定 |
|---|---|---|
| 肩胯 | 肩窩 ↔ 胯(股関節の奥) ※補正点、下記参照 | 水平ズレ |
| 肘膝 | 肘 ↔ 膝 | 水平ズレ |
| 手足 | 手首 ↔ 足首 | 水平ズレ |
| 中定線 | 肩の中点 ↔ 胯の中点の垂線 | 鉛直からの傾き |

**見ないもの:** 内三合(心与意合・意与気合・気与力合)は映像に映らないため直接は測れない。ただし上下相随・節節貫穿の崩れ(動きのカクつき=断勁のサイン)として間接的に映像に現れうる。将来の拡張候補。

## 設計上の決定事項

**肩窩・胯の補正点。** AIの検出点は体表推定なので、流派の身体観(肩窩、大腿骨頭が骨盤に差し込まれる奥)に合わせ、検出点から体の中心方向へ寄せた補正点を計算して使う。初期値: 肩の内寄せ12% / 胯の内寄せ22% / 胯の沈み5%(胴長比)。アプリ内スライダーで調整でき、端末に保存される。**校正方法:** 正面から撮った動画で、確信をもって坐胯できているコマを静止表示し、点が身体感覚と重なるまでスライダーを合わせる。

**しきい値(調整可能)。** 水平ズレは胴長(肩中点〜胯中点)に対する比で判定: 初期値は 緑 < 0.28 ≤ 黄 < 0.55 ≤ 赤。調整パネルのスライダーで変更でき、端末に保存される。稽古仲間のフィードバックをもとに各自が校正する運用。中定線の傾き: 緑 < 4° ≤ 黄 < 8° ≤ 赤(固定)。

**判定は2Dであることの限界。** 水平方向のズレのみを見るため撮影角度の影響を受ける。左右の対称性・中定は正面から、肘膝の前後対応は真横から撮るのが読みやすい。校正は正面撮りで行う。

**リアルタイム表示は意図的に後回し。** 套路中に画面を見ること自体が意念を散らすため、「打つときは打つ、あとで映像と向き合う」を基本設計とした。

## 技術メモ

- ポーズ推定: MediaPipe Tasks Vision `@mediapipe/tasks-vision@0.10.14`(CDN)、モデルは `pose_landmarker_full`、GPU→CPUフォールバック
- 検出は再生中フレームごと(`requestVideoFrameCallback`)。タイムスタンプは `performance.now()`(逆方向シーク対策)
- 全33ランドマーク中、肩11/12・肘13/14・手首15/16・胯23/24・膝25/26・足首27/28 を使用
- UI: スロー再生(1×/0.5×/0.25×)、±1コマ送り(1/30秒)、シークバー、左右・骨格・中定線の表示切替、ズレ数値チップ
- 校正値は `localStorage` に保存(キー: `sangokyo-cal`)

## ロードマップ

**第1段階(完了): 37式・体の外三合モード** — 上記すべて。

**第1.2段階(完了): 比較モード** — 単体/比較(2画面)のタブ切替。比較は先生と自分の動画を縦に並べ、それぞれ独立に再生・シーク・±1コマ送りして同じ姿勢で止めて見比べる方式。再生速度・表示切替・校正・しきい値は両画面共通。処理を軽くするため比較モードは軽量モデル(pose_landmarker_lite)を使用。同期再生(基準点合わせ)は将来課題。

**第1.5段階(次): 剣54式モード**
- 剣尖はポーズ推定では検出不可のため別トラッキング。方式A(推奨): 剣尖付近に蛍光色マーカーを巻き、色追跡。方式B: 最初のフレームで剣尖をタップ指定→映像追跡(高速の剣で外れやすい)
- 剣筋の可視化: 剣尖の軌跡を残像(長時間露光風の光の線)として描画。軌跡の色を剣尖速度で変え、勁の途切れ(不自然な減速)を浮かび上がらせる
- 体の外三合の線と剣筋を同時表示し、腰胯の回転と剣の弧の連動を見る
- モード切替で同一アプリ内に共存。追跡点は増設可能なデータ構造にする

**将来の拡張候補(順不同)**
- しきい値の実測ベース調整、撮影角度(正面/側面)モードの明示切替
- 関節速度の滑らかさから断勁的なカクつきを検出
- 老師の動画との並列比較
- 太極拳翻訳スキル(taijiquan-translation)の術語対照表と連携した言葉によるフィードバック(例: 沉肩墜肘の崩れの指摘)
- リアルタイム鏡モード(カメラ入力)

## 変更履歴

**ver 1.1** — 肩・胯の水平線を追加(左右の傾き=中定の横方向のチェック。水平からの傾き角度で色が変わる)。数値チップに肩・胯の傾き角度(°)を追加。操作メニュー(シーク・再生・コマ送り・速度)を動画直下にまとめ、数値チップをその下へ移動。速度切替を各動画ごとに操作可能に。タイトルにバージョン表示。

**ver 1.0** — 最初の配布版。外三合の3ペア(肩胯・肘膝・手足)+中定線、肩窩・胯の補正点と校正スライダー、判定しきい値スライダー、単体/比較(2画面)モード、スロー再生・コマ送り、スクショ付きマニュアル(manual.html)。稽古仲間へ公開。

## 開発の再開方法(メモ)

Claudeとの新しいチャットで、このリポジトリの **公開URLとこのREADME** を伝えれば、どのモデルでも現物のコードを取得して続きから開発できる。ファイルはチャットをまたいで残らないため、**GitHubが常に「正」**。更新は改良版 `index.html` を同名でアップロードして上書き(反映まで1〜2分)。
